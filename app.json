{
  "scripts": {
    "dokku": {
      "predeploy": "php bin/console doctrine:migrations:migrate --allow-no-migration && php bin/console doctrine:fixtures:load --no-interaction",
      "postdeploy": "php bin/console app:updateplayers && php bin/console app:syncfranchisedata"
    }
  }
}